import "./Navbar.css";

const Navbar = () => {
  return (
    <div id="navbar">
      <div>
        <span>Notes App</span>
        <button className="btn btn-success">Crear Nota</button>
      </div>
    </div>
  );
};

export default Navbar;
